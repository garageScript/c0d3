image: node:10.14.1

stages:
  - test
  - build
  - deploy

cache:
  key: use-same-cache-for-all-branches-and-jobs
  paths:
  - node_modules/

test:
  stage: test
  script:
    - yarn ci:test
  only:
  - merge_requests

build:
  stage: build
  script:
    - yarn ci:build
  only:
  - master

deploy:
  stage: deploy
  script:
    - yarn ci:deploy
  environment:
    name: production
    url: https://c0d3.com
  only:
  - master

